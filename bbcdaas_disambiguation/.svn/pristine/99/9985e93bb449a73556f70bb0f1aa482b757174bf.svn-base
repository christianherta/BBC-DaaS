package de.bbcdaas.disambiguation.core.configs;

import de.bbcdaas.disambiguation.core.connector.AbstractConnector;

/**
 * Base class for all configurations of data exporter.
 * @author Robert Illers
 */
public class AbstractDataExporterConfig<T extends AbstractConnector> extends AbstractConfiguration<T> {
    
	/**
	 * Constructor with connector to the persistence layer as parameter
	 * @param connector 
	 */
    public AbstractDataExporterConfig(T connector) {
        super(connector);
    }
}
