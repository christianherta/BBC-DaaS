CREATE TABLE term(
	termId INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1),
	termValue VARCHAR(255) NOT NULL,
	termFrequency INTEGER NOT NULL,
	UNIQUE (termValue)
);

CREATE TABLE termBlacklist(
	termId INTEGER,
	PRIMARY KEY (termId)
);